#include "main.h"
#include <stdio.h>

/**
 * _puts - prints string followerd by a newline
 *
 * @str: input parameter for string
 * Return: returns string value
 */

void _puts(char *str)
{
	puts(str);
}
