#include "main.h"
#include <stdio.h>

/**
 * _memset - mem set
 * @s: input par
 * @b: input par
 * @n: input par
 * Return: return s.
 */
char *_memset(char *s, char b, unsigned int n)
{
	return (s);
}
