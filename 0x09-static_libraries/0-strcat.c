#include "main.h"
#include <stdio.h>

/**
 * _strcat - function that concatenates two strings
 * @dest: input parameter
 * @src: input parameter
 * Return: Always 0.
 */

char *_strcat(char *dest, char *src)
{
	return (dest);
}
