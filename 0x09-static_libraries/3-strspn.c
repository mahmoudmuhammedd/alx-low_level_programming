#include "main.h"
#include <stdio.h>

/**
 * _strspn - function that gets the length of a prefix
 * @s: input
 * @accept: input
 * Return: Always 0.
 */
unsigned int _strspn(char *s, char *accept)
{
}
