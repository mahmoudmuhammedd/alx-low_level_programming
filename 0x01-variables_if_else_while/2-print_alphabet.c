#include <stdio.h>

/**
 * main - Entry point
 *
 * Description: Alphabet printer
 *
 * Return: Always 0 (Success)
 */

int main(void)

{
int ch
for (ch = 97; ch < 123; ch++)
{
	putchar(ch);
}
return (0);
}
