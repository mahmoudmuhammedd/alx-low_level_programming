#include "main.h"

/**
 * islower - detects lower case
 *
 * Return: Always 0 (Success)
 */

int _islower(int c)
{
	char ch[27];
	int c;

	for (c = 0; c < 26; c++)
	{
	char[ch] = 'a' + c;
	}

	arr[ch] = '\0';
	islower();
}
